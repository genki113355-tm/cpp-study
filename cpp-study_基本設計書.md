# シロクマC++ラボ (cpp-study) 基本設計書

| 項目 | 内容 |
| :--- | :--- |
| **ドキュメント名** | シロクマC++ラボ システム基本設計書 |
| **プロジェクト名** | cpp-study (シロクマC++ラボ) |
| **公式本番URL** | [https://shirokuma-cpp.jp/](https://shirokuma-cpp.jp/) |
| **リポジトリ** | `genki113355-tm/cpp-study` (GitHub) |
| **作成日** | 2026年9月21日 |
| **バージョン** | 1.0.0 |
| **ステータス** | 正式版 (Production Ready) |

---

## 目次

1. [システム概要](#1-システム概要)
   - 1.1 背景と目的
   - 1.2 ターゲットユーザーと提供価値
   - 1.3 サイト基本情報
2. [システムアーキテクチャ](#2-システムアーキテクチャ)
   - 2.1 全体アーキテクチャ図
   - 2.2 採用技術スタック
   - 2.3 ディレクトリ構造と責務
3. [機能要件・機能設計](#3-機能要件機能設計)
   - 3.1 カリキュラム学習システム（44記事・章遷移・差分解説）
   - 3.2 アーキテクチャ可視化システム（UML図・メモリマップ）
   - 3.3 ブラウザ内インベーダーゲームエミュレータ
   - 3.4 C++オンライン実行ラボ（Playground & 演習チャレンジ）
   - 3.5 学習進捗管理・マイルストーン修了証システム
   - 3.6 キャラクターガイダンスシステム
   - 3.7 4姉妹メディア相互リンク連携
4. [データモデル・データ設計](#4-データモデルデータ設計)
   - 4.1 主要エンティティ定義
   - 4.2 コーストラック・章コード体系
   - 4.3 進捗ストレージ仕様 (localStorage)
5. [UI/UX・画面設計](#5-uiux画面設計)
   - 5.1 画面一覧
   - 5.2 共通レイアウト構造
   - 5.3 レスポンシブ設計方針（モバイル・タブレット・PC）
   - 5.4 デザインシステム・カラーパレット
6. [SEO・クローラビリティ設計](#6-seoクローラビリティ設計)
   - 6.1 SSG / Prerender 静的生成アーキテクチャ
   - 6.2 メタタグ・Canonical・OGP 設計
   - 6.3 Schema.org 構造化データ (JSON-LD)
   - 6.4 クローラー向け初期HTMLフォールバック
   - 6.5 サイトマップ & RSS フィード生成
7. [非機能要件・セキュリティ・運用](#7-非機能要件セキュリティ運用)
   - 7.1 パフォーマンス・最適化設計
   - 7.2 セキュリティ設計
   - 7.3 アクセシビリティ対応
   - 7.4 解析・広告基盤
8. [ビルド・デプロイ・運用保守フロー](#8-ビルドデプロイ運用保守フロー)
   - 8.1 パッケージスクリプト一覧
   - 8.2 ビルド & 静的生成パイプライン
   - 8.3 デプロイフロー (Cloudflare Pages + Git)

---

## 1. システム概要

### 1.1 背景と目的
C++は現代のゲームエンジン、組み込み、リアルタイム制御、ロボティクス、高性能計算などあらゆる基盤領域で圧倒的なシェアを誇るプログラミング言語です。しかしながら、多くの入門書籍や学習サイトは「文法の暗記」や「断片的なサンプルコード」にとどまり、実務で最も重要となる「オブジェクト指向設計」「リファクタリング」「メモリ安全管理」「アーキテクチャ設計」を体系的かつ直感的に学べるメディアは極めて稀でした。

本プロジェクト「**シロクマC++ラボ**」は、**1本の固定画面レトロシューティングゲーム（インベーダーゲーム風: RETRO SPACE SHOOTER）を開発教材**とし、未熟な「レガシーC++（生ポインタ・手動メモリ管理・巨大main関数・菱形継承）」から、堅牢で拡張性の高い「モダンC++（スマートポインタ・RAII・ECS設計・TDD・UML設計書）」へと段階的にリファクタリングしながら進化させていく実践型Web教育プラットフォームです。

### 1.2 ターゲットユーザーと提供価値

| ターゲット層 | 主な課題・悩み | 当メディアが提供する解決策 |
| :--- | :--- | :--- |
| **C言語・初級C++経験者** | ポインタ演算や手動メモリ管理でバグを多発させてしまう | レガシー設計の落とし穴を実体験し、RAIIやスマートポインタによる近代的な安全設計を体得 |
| **ゲームプログラミング志望者** | ゲームの設計が肥大化し、スパゲッティコードで破綻する | 段階的リファクタリングを通じて、ポリモーフィズムやECS（Entity Component System）設計を習得 |
| **組み込み・実務C++エンジニア** | レガシー資産のモダナイズ手法やTDD（テスト駆動開発）が分からない | GoogleTestによる単体テスト、UMLクラス図、メモリマップ可視化を用いた実践的アプローチを提供 |
| **他言語エンジニア (Java/C#/Python)** | C++特有のメモリモデル（スタック/ヒープ、所有権、ムーブセマンティクス）が曖昧 | 視覚的なメモリモデル図解と、対話型キャラクターによる徹底的な言語差分解説 |

### 1.3 サイト基本情報
- **正式名称**: シロクマC++ラボ 〜ゲーム開発で学ぶオブジェクト指向開発 レガシー設計からモダン設計まで〜
- **本番ドメイン**: `https://shirokuma-cpp.jp/`
- **ホスティング**: Cloudflare Pages
- **運営主体**: シロクマC++ラボ 開発・編集チーム
- **姉妹メディア**:
  - シロクマC++自動化ラボ (`https://shirokuma-auto-cpp.jp/`)
  - シロクマQt×C++ラボ (`https://shirokuma-qt-cpp.jp/`)
  - 水中音響・ソナー技術入門 (`https://sonar-guide.jp/`)

---

## 2. システムアーキテクチャ

### 2.1 全体アーキテクチャ図

```
+-----------------------------------------------------------------------------------+
|                                 クライアント (Webブラウザ)                          |
|  - SPAインタラクティブUI (React 18 + Tailwind CSS)                                |
|  - HTML5 Canvas ゲームエミュレータ (RETRO SPACE SHOOTER)                          |
|  - オンラインC++ Playground (コード実行・チャレンジ)                             |
|  - ローカル進捗ストレージ (localStorage) & 修了証生成 (Canvas)                    |
+-----------------------------------------------------------------------------------+
                                          ▲
                                          │ HTTPS (エッジCDNキャッシュ)
                                          ▼
+-----------------------------------------------------------------------------------+
|                           Cloudflare Pages (グローバルエッジCDN)                   |
|  - dist/index.html (初期静的フォールバック内包)                                   |
|  - dist/{slug}/index.html (全44記事の完全事前生成HTML)                             |
|  - feed.xml (RSS 2.0) & sitemap.xml (SEOサイトマップ)                            |
|  - 最適化済みアセット (JSバンドル, CSS, WebP/SVG画像)                            |
+-----------------------------------------------------------------------------------+
                                          ▲
                                          │ npm run deploy (Wrangler CLI)
                                          ▼
+-----------------------------------------------------------------------------------+
|                              ビルド & 静的事前生成パイプライン                      |
|  1. TypeScript 型検査 (tsc)                                                       |
|  2. Vite 本番バンドル (Vite 6, Rollup Code Splitting, Terser Minify)              |
|  3. SEO Prerender Generator (node scripts/generate-seo.mjs)                       |
|     - 全44記事のセマンティックHTML注入 (SSRライクな事前生成)                      |
|     - Schema.org JSON-LD 構造化データ埋込                                         |
|     - 静的クローラー向け公式姉妹サイト相互リンク自動注入                          |
+-----------------------------------------------------------------------------------+
```

### 2.2 採用技術スタック

| 分類 | 技術名 | バージョン | 採用理由・役割 |
| :--- | :--- | :--- | :--- |
| **フロントエンドコア** | React / React-DOM | 18.3.1 | コンポーネント指向、宣言的UI、コード分割（Suspense/Lazy）による高速化 |
| **言語** | TypeScript | ~5.7.2 | 厳格な型安全性、カリキュラム・章データの不整合防止 |
| **ビルドツール** | Vite | ^6.2.0 | 高速HMR（開発環境）、Rollupベースの最適化バンドル（本番環境） |
| **スタイル・UI** | Tailwind CSS | ^3.4.17 | ユーティリティファースト、ゼロランタイム、ダークモード/サイバーパンク基調のデザイン |
| **CSSユーティリティ** | clsx / tailwind-merge | 最新 | 条件付きクラス結合と競合解消 |
| **アイコン** | Lucide React | ^1.16.0 | 軽量で統一されたSVGアイコン群 |
| **シンタックスハイライト**| Prism.js | ^1.30.0 | C++、CMake、JSON、Shellの高速・軽量な構文着色 |
| **演出効果** | canvas-confetti | ^1.9.4 | マイルストーン達成・全章修了時の祝賀紙吹雪アニメーション |
| **インフラ / CDN** | Cloudflare Pages | - | グローバルエッジ配信、超高速TTFB、無制限帯域、SSL自動化 |
| **CLI / デプロイ** | Wrangler | ^4.131.1 | Cloudflare Pagesへの直接コマンドラインデプロイ |
| **SEO生成** | Node.js (ESM) | 22.x | SSR事前生成、sitemap.xml、RSS 2.0生成スクリプト |

### 2.3 ディレクトリ構造と責務

```
cpp-study/
├── index.html                  # SPAメインHTML (クローラー向けフォールバック内包)
├── package.json                # プロジェクト定義・依存関係・デプロイスクリプト
├── tsconfig.json               # TypeScriptコンパイラ設定
├── vite.config.ts              # Viteビルド設定 (チャンク分割・手動チャンク最適化)
├── tailwind.config.js          # Tailwindテーマ拡張 (カラーパレット・アニメーション)
├── public/                     # 静的公開アセット
│   ├── images/                 # キャラクター画像、OGP画像、図解画像
│   ├── favicon.svg             # サイトアイコン (シロクマ 🐻‍❄️)
│   ├── feed.xml                # 自動生成されるRSS 2.0フィード
│   └── sitemap.xml             # 自動生成されるXMLサイトマップ
├── scripts/
│   └── generate-seo.mjs        # 全記事静的事前生成・SEO自動生成スクリプト
└── src/
    ├── main.tsx                # アプリケーション初期化・エントリポイント
    ├── App.tsx                 # ルートコンポーネント (ルーティング・モーダル管理)
    ├── index.css               # グローバルCSS・Tailwindディレクティブ・Prismテーマ
    ├── components/
    │   ├── layout/             # 共通レイアウト系 (Navbar, Sidebar, Footer, Modals)
    │   ├── curriculum/         # カリキュラム学習ビュー (ChapterView, TopPageView等)
    │   │   └── uml/            # UMLダイアグラム可視化コンポーネント
    │   ├── emulator/           # HTML5 Canvas ゲームエミュレータ
    │   ├── playground/         # C++オンライン実行ラボ・コードチャレンジ
    │   ├── common/             # 汎用UIパーツ (バッジ, タブ, ツールチップ等)
    │   └── affiliate/          # スポンサー・技術書籍リンクバナー
    ├── data/
    │   ├── chapters.ts         # 全カリキュラム記事・章データ定義
    │   ├── chapterMetadata.ts  # 章メタデータ・コード体系・トラック管理
    │   ├── codingChallenges.ts # C++実践演習チャレンジ問題集
    │   └── guides.ts           # C++基本文法総覧・特別ガイドデータ
    ├── hooks/
    │   └── useSEO.ts           # 動的メタタグ (title, desc, canonical, OGP) 同期フック
    ├── types/
    │   └── curriculum.ts       # カリキュラム・章・UML・クイズ等の型定義
    └── utils/
        └── progressManager.ts  # 学習進捗 (localStorage) の保存・読込・サニタイズ
```

---

## 3. 機能要件・機能設計

### 3.1 カリキュラム学習システム（44記事）

本メディアの中核となる学習システムです。全44記事は以下の4大トラックで体系化されています。

```
[学習トラック全体マップ]
┌─────────────────────────────────────────────────────────────┐
│ 🏛️ レガシーC++編 (Classic Track: C1〜C6)                   │
│   ・C++03の生ポインタ、手動メモリ管理、菱形継承、CRTP       │
└──────────────────────────────┬──────────────────────────────┘
                               │ 進化 (Refactoring)
                               ▼
┌─────────────────────────────────────────────────────────────┐
│ 🚀 モダンC++編 (Modern Track: M1〜M6)                       │
│   ・C++17/20、unique_ptr、RAII、ECS設計、GoogleTest TDD     │
└──────────────────────────────┬──────────────────────────────┘
                               │ 拡張 (Deep Dive)
               ┌───────────────┴───────────────┐
               ▼                               ▼
┌──────────────────────────────┐ ┌──────────────────────────────┐
│ 🧭 設計書・文法編            │ │ 💡 現場ノウハウ・特別ガイド  │
│ (Reading Track: R1〜R8)      │ │ (Guide Track: G1〜G24)       │
│ ・UML完全設計書、C++総覧     │ │ ・メモリ解析、CI/CD、CMake   │
└──────────────────────────────┘ └──────────────────────────────┘
```

#### 主な提供機能:
- **ビフォー / アフターコード比較**:
  同一画面上でレガシーコードとモダンリファクタリングコードをタブ切り替え、または並列表示し、設計の差異を一目で理解可能。
- **行解剖解説 (Line Breakdown)**:
  コード内の特定の重要行（初期化子リスト、`noexcept`、ムーブ代入など）にピン留めされた「超訳」「トークン分解」「落とし穴（書かないとどうなるか）」を展開。
- **理解度チェッククイズ**:
  章の末尾に3択〜4択の対話型クイズを配備。即時正誤判定とシロクマ先生による詳細な解説を提供。

---

### 3.2 アーキテクチャ可視化システム

#### 1. UMLダイアグラムビューア (`UmlDiagramViewer.tsx`)
- **クラス図**: クラス名、ステレオタイプ、属性（可視性 `+`, `-`, `#`）、操作、およびクラス間リレーション（関連、集約、コンポジション、汎化、実現）をSVG描画。
- **シーケンス図**: ゲームループ内のメッセージ送受信（プレイヤー入力 → 更新 → 描画）を時系列で視覚化。
- **状態遷移図**: ゲームステート（タイトル → プレイ中 → ポーズ → ゲームオーバー）の遷移イベントとガード条件を図解。

#### 2. メモリマップビジュアライザ (`MemoryVisualizer.tsx`)
- スタック領域（ローカル変数、ポインタ変数、戻り先アドレス）とヒープ領域（`new` / `malloc` で確保されたオブジェクト）のアドレス配置とライフサイクルをASCII・グラフィカル両面で可視化。
- ダングリングポインタ、二重解放（Double Free）、メモリリークの発生メカニズムを視覚的に解説。

---

### 3.3 ブラウザ内インベーダーゲームエミュレータ (`GameEmulator.tsx`)

HTML5 Canvas API を用いて完全内製された、インベーダー風シューティングゲームエミュレータです。

- **目的**: カリキュラム内で解説されているC++の設計思想（C1の単一main関数構造から、M6のECSコンポジション構造まで）が、ゲームの挙動としてどのように反映されているかをブラウザ上でリアルタイム体験。
- **機能**:
  - プレイヤー移動（左右キー / A・Dキー）、レーザー発射（Spaceキー）。
  - 敵キャラクター（インベーダー）の編隊移動、UFOボーナス、迎撃ビーム。
  - 防壁シールドの破壊判定、スコア/残機システム。
  - バージョン切り替え機能（レガシー挙動 vs モダン拡張挙動）。

---

### 3.4 C++オンライン実行ラボ (`OnlinePlaygroundModal.tsx`)

外部環境構築なしに、ブラウザ上でC++コードの挙動をシミュレーション・検証できるサンドボックスです。

- **コードエディタ**: 行番号表示、シンタックスハイライト、コードフォーマット補助。
- **コーディングチャレンジ (`codingChallenges.ts`)**:
  - 「生ポインタを `std::unique_ptr` に書き換えよ」
  - 「仮想関数テーブルを用いたポリモーフィズムを実装せよ」
  - 「メモリプールアロケータの断片化を防げ」
  等の実務直結課題を出題し、リアルタイム判定を実施。

---

### 3.5 学習進捗管理・マイルストーン修了証システム

ユーザーの学習継続モチベーションを最大化するゲーミフィケーション基盤です。

- **進捗追跡**: 各章の読了チェックボタンで進捗率（%）を即時計算。
- **ローカル永続化**: サーバーログイン不要で、ブラウザの `localStorage` に安全に状態を保存。
- **公式修了証発行モーダル (`MilestoneModal.tsx`)**:
  - 一定章数を修了するごとにマイルストーンバッジを付与。
  - 全章達成時に、受講者名（任意入力）入りの「公式デジタル修了証」をCanvasで動的生成・PNG保存可能。
  - 他ブラウザや端末間で進捗を引き継げる「進捗インポート / エクスポート (JSON)」機能を完備。

---

### 3.6 キャラクターガイダンスシステム

学習の敷居を下げ、飽きさせないためのメンター対話システムです。

- **シロクマ先生 (Sensei)**: 低レイヤ・数理アルゴリズム・C++規格の専門家。本質的な設計意図や深い理論をやさしく解説。
- **ペンギンくん (Penguin)**: 手動評価やバグに苦しむ若手エンジニア。初学者が疑問に思うポイントを代弁して質問。
- **感情ステート**: `normal`, `smug`, `shocked`, `thinking`, `teaching`, `happy`, `sweating` などの感情に応じてアイコンや吹き出しデザインが連動。

---

### 3.7 4姉妹メディア相互リンク連携

シロクマ学習エコシステムとして、以下の4サイトがヘッダー、サイドバー、フッター、トップページにて強固な相互リンク網を構築しています。

```
                    ┌────────────────────────┐
                    │    シロクマC++ラボ     │
                    │   (shirokuma-cpp.jp)   │
                    │ 【ゲーム開発・C++設計】│
                    └───────────┬────────────┘
                                │ 相互リンク
          ┌─────────────────────┼─────────────────────┐
          ▼ 相互リンク           ▼ 相互リンク           ▼ 相互リンク
┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│シロクマC++自動化 │  │ シロクマQt×C++   │  │ 水中音響・ソナー │
│      ラボ        │  │       ラボ       │  │    技術入門      │
│(shirokuma-auto-  │  │(shirokuma-qt-    │  │ (sonar-guide.jp) │
│     cpp.jp)      │  │     cpp.jp)      │  │【音響・物理・信号│
│【Docker/CI/自動化│  │【Linux/Qt/HMIGUI】│  │    処理数理】    │
└──────────────────┘  └──────────────────┘  └──────────────────┘
```

---

## 4. データモデル・データ設計

### 4.1 主要エンティティ定義 (`src/types/curriculum.ts`)

```typescript
// 1. 章データ定義
export interface Chapter {
  id: number;                          // 章の一意なID (1〜44)
  slug: string;                        // URLスラッグ (e.g. 'chapter-1-spaghetti-code')
  category?: ContentCategory;          // 'architecture' | 'reading' | 'guide' | 'column'
  courseTrack?: CourseTrack;           // 'classic' | 'modern' | 'reading' | 'guide'
  courseChapterCode?: string;          // 目次用コード ('C1'〜'C6', 'M1'〜'M6', 'R1'〜'R8')
  title: string;                       // 章タイトル
  subtitle: string;                    // 章サブタイトル
  badge: string;                       // バッジ表示名 (e.g. 'レガシー第1章')
  description: string;                 // 章の概要文
  gameVersion?: string;                // 連動するゲームエミュレータのバージョン
  sections: SectionContent[];          // 章を構成するセクション配列
  quiz?: QuizQuestion[];               // 章末クイズ配列
  nextChapterSlug?: string;            // 次章リンクスラッグ
  prevChapterSlug?: string;            // 前章リンクスラッグ
}

// 2. セクションデータ定義
export interface SectionContent {
  id: string;                          // セクションID
  title: string;                       // セクション見出し
  leadText?: string;                   // 導入リード文
  dialogueBefore?: DialogueItem[];     // セクション冒頭のキャラクター対話
  codeFiles?: CodeFile[];              // ソースコードファイル配列
  paradigmComparison?: ParadigmComparison; // C vs C++ パラダイム比較
  memoryMap?: MemoryMapDoc;            // メモリマップ図解
  umlDiagram?: UmlDiagramDoc;          // UML設計図データ
  explanationText?: string;            // 詳細解説本文 (Markdown/HTML)
  dialogueAfter?: DialogueItem[];      // セクション末尾のまとめ対話
  takeaways?: KeyTakeaway[];           // 重要ポイント・まとめ
}

// 3. コードファイル定義
export interface CodeFile {
  filename: string;                    // ファイル名 (e.g. 'SpaceShooter.cpp')
  language: string;                    // 言語 ('cpp', 'cmake', 'h')
  description: string;                 // コードの説明
  code: string;                        // コード本文
  highlightLines?: number[];           // 強調表示する行番号配列
  diffType?: 'original' | 'refactored' | 'added'; // 差分種別
  lineExplanations?: CodeExplanationItem[];       // 行単位の深掘り解剖データ
}
```

### 4.2 コーストラック・章コード体系

| トラックID | トラック名 | コード体系 | 収録範囲 | 主な学習テーマ |
| :--- | :--- | :--- | :--- | :--- |
| `classic` | 🏛️ レガシーC++編 | `C1` 〜 `C6` | 全6章 | スパゲッティコード、クラス化、生ポインタ・手動メモリ管理、菱形継承、CRTP、手動ループ |
| `modern` | 🚀 モダンC++編 | `M1` 〜 `M6` | 全6章 | C++17規格、スマートポインタ（unique/shared）、RAII、多態性・インターフェース、ECS設計、GoogleTest |
| `reading` | 🧭 設計書・文法編 | `R1` 〜 `R8` | 全8章 | UMLクラス図・シーケンス図・状態遷移図の完全設計書、C++基本文法・逆引きチートシート |
| `guide` | 💡 特別ガイド編 | `G1` 〜 `G24`| 全24章 | メモリプール、自作アロケータ、マルチスレッド、CMake入門、Gitワークフロー等 |

### 4.3 進捗ストレージ仕様 (localStorage)

ユーザーの学習進捗はクライアント側の `localStorage` に保持されます。外部入力による改ざんや破損を防ぐためのサニタイズ処理が実装されています。

- **キー名**: `cpp_study_completed_chapters`
- **データ形式**: `number[]` の JSON シリアライズ文字列（例: `"[1, 2, 3, 7]"`）
- **サニタイズ規則 (`progressManager.ts`)**:
  - 配列でないデータやパース失敗時は即座に空配列 `[]` にフォールバック。
  - 実在する章ID (`VALID_CHAPTER_IDS`) に含まれない不正な数値は除外。
  - 重複する数値は `Array.from(new Set(...))` で一意化。

---

## 5. UI/UX・画面設計

### 5.1 画面一覧

| 画面名 | パス / URL | 主な役割・機能 |
| :--- | :--- | :--- |
| **トップページ** | `/` | サイト概要、ヒーローセクション、ゲーム進化ロードマップ、学習トラック選択、姉妹サイト案内 |
| **チャプター詳細画面** | `/{slug}` | 各章の解説、コード比較、メモリマップ、UML図、エミュレータ起動、理解度クイズ |
| **ソースコードガイドモーダル** | (モーダル) | プロジェクト全ファイルのソースツリー閲覧、ダウンロード案内 |
| **オンラインPlaygroundモーダル** | (モーダル) | ブラウザ内C++コード編集・実行シミュレータ、コーディング課題挑戦 |
| **公式修了証モーダル** | (モーダル) | 学習達成度表示、公式修了証PNG生成、進捗JSONエクスポート/インポート |
| **プライバシーポリシーモーダル** | (モーダル) | アクセス解析（GA4）、広告配信（AdSense）、免責事項の明記 |

### 5.2 共通レイアウト構造

```
+-----------------------------------------------------------------------------------+
| Navbar: サイトロゴ | カリキュラム進捗率 | コードラボ起動 | ソース | 修了証 | ☰メニュー|
+-----------------------------------------------------------------------------------+
|  Sidebar (目次)   |  Main Content Area                                            |
|                   |                                                               |
|  - 達成率プログレス|  - 章ヘッダー (バッジ, タイトル, サブタイトル, 担当領域)        |
|  - Classic トラック|  - キャラクター導入対話 (シロクマ先生 & ペンギンくん)           |
|  - Modern トラック |  - 本文解説 / パラダイム比較 / メモリマップ可視化              |
|  - Reading トラック|  - ソースコード比較 (タブ切り替え, 差分ハイライト, 行解剖)     |
|  - Guide トラック  |  - ゲームエミュレータ実行領域 (Canvas)                         |
|  - 姉妹サイトリンク|  - 章末理解度クイズ (正誤判定・解説)                           |
|  - キャラクター紹介|  - 次章・前章ナビゲーションボタン                              |
+-----------------------------------------------------------------------------------+
| Footer: コピーライト | 姉妹サイト3枠並列リンク | プライバシーポリシーリンク       |
+-----------------------------------------------------------------------------------+
```

### 5.3 レスポンシブ設計方針

| 端末区分 | 画面幅ブレークポイント | レイアウト挙動 |
| :--- | :--- | :--- |
| **スマートフォン** | `< 768px` (320px〜414px) | - サイドバーはオフキャンバス（ドロワー）化。ハンバーガーボタンで開閉。<br>- ドロワー幅上限を `85vw` に設定し、タップで閉じる背景オーバーレイを配備。<br>- コードブロック、テーブル、数式は `overflow-x-auto` で画面外はみ出しを防止。<br>- ヒーロー見出しやボタンパディングを小型画面用に縮小最適化。 |
| **タブレット** | `768px 〜 1024px` | - サイドバーを表示可能にし、メイン領域の余白を適正化。 |
| **PC・デスクトップ**| `>= 1024px` | - 左側固定サイドバー（スクロール追従）＋右側広域コンテンツエリアの2カラム構成。<br>- コードエディタと解説の並列配置に対応。 |

### 5.4 デザインシステム・カラーパレット

暗所での長時間のコードリーディングに耐えうる、サイバーパンク・ディープネイビー調のカラーパレットを採用しています。

- **Primary Background**: `#090d16` / `#0c121e`（深遠な宇宙・サイバーネイビー）
- **Card / Surface**: `#0f172a` / `#1e293b`（スレートネイビー）
- **Accent Cyan (主アクセント)**: `#38bdf8` / `#06b6d4`（先進的なC++・氷河を想起させるシアン）
- **Accent Emerald (成功・モダン)**: `#10b981` / `#34d399`（モダンC++、合格状態）
- **Accent Amber (警告・レガシー)**: `#f59e0b` / `#fbbf24`（レガシーC++、注意喚起、トロフィー）
- **Text Main**: `#f8fafc`（高コントラストホワイト）
- **Text Muted**: `#94a3b8` / `#64748b`（可読性を維持したスレートグレー）

---

## 6. SEO・クローラビリティ設計

### 6.1 SSG / Prerender 静的生成アーキテクチャ

SPA（Single Page Application）でありながら、検索エンジンクローラー（Googlebot等）やSNSボット（Twitter/X, Facebook）に対して完全に最適化されたSSR同等のHTMLを提供する独自ジェネレーター（`scripts/generate-seo.mjs`）を内蔵しています。

```
[ビルド実行時: npm run build]
   │
   ├─ 1. Vite Build ──> dist/index.html (クライアント用JS/CSSバンドル生成)
   │
   └─ 2. generate-seo.mjs
         │
         ├─ src/data/chapters.ts から全44記事のメタデータをロード
         │
         ├─ 各記事について dist/{slug}/index.html を生成
         │    - 固有の <title>, <meta description>, <link canonical> を注入
         │    - OGP, Twitter Card を個別設定
         │    - Schema.org (TechArticle, BreadcrumbList, FAQPage) をJSON-LD注入
         │    - <div id="root"> 内に静的セマンティック解説 ＋ 姉妹サイト公式リンクを注入
         │
         ├─ public/sitemap.xml (全ページのXMLサイトマップ) を生成
         └─ public/feed.xml (RSS 2.0 フィード) を生成
```

### 6.2 メタタグ・Canonical・OGP 設計

- **Canonical URL の厳格化**:
  トップページは `https://shirokuma-cpp.jp/`、各記事は `https://shirokuma-cpp.jp/{slug}` を厳格に指定し、URLパラメータやハッシュによる重複インデックスを排除。
- **動的メタ同期 (`useSEO` フック)**:
  ブラウザ側でSPA画面遷移（`pushState`）が発生した際も、`document.title`、`description`、`canonical`、`OGP` をリアルタイム更新。

### 6.3 Schema.org 構造化データ (JSON-LD)

Google リッチリザルトに対応した構造化データを完全出力します。

- **WebSite**: サイト名、URL、および姉妹メディア群（`sameAs` による相互関連付け）。
- **Course**: 「ゲーム開発で学ぶC++オブジェクト指向設計マスター講座」としての教育コース定義。
- **TechArticle**: 各記事の技術解説記事定義（見出し、概要、著者、公開者）。
- **BreadcrumbList**: `TOP > カリキュラム種別 > 記事名` のパンくず階層構造。
- **FAQPage**: 章末理解度クイズをQ&A形式のリッチスニペットとして検索結果に露出。

### 6.4 クローラー向け初期HTMLフォールバック

JavaScriptを実行しない監査スクリプト、curl、および初期インデックスクローラーに対し、`index.html` の `<div id="root">` 内および `<noscript>` に以下の要素を静的埋め込みしています。

1. **サイトタイトルと包括的概要文**
2. **カリキュラムの学習ロードマップ概要**
3. **公式姉妹メディアへの `<a href="...">` 相互リンク**
   - シロクマC++自動化ラボ (`https://shirokuma-auto-cpp.jp/`)
   - シロクマQt×C++ラボ (`https://shirokuma-qt-cpp.jp/`)
   - 水中音響・ソナー技術入門 (`https://sonar-guide.jp/`)

> [!NOTE]
> ブラウザでReactがロードされると、`ReactDOM.createRoot().render()` により `#root` 内の初期静的HTMLは即座にリッチな対話型アプリケーションに置換されます。ユーザーの視覚的体験を一切阻害しません。

---

## 7. 非機能要件・セキュリティ・運用

### 7.1 パフォーマンス・最適化設計
- **Code Splitting (遅延ロード)**:
  巨大なエミュレータ（`GameEmulator`）、Playground（`OnlinePlaygroundModal`）、修了証モーダル（`MilestoneModal`）、ソースコードガイド（`SourceModal`）は `React.lazy()` を用いて必要時のみ動的インポート。
- **Vite 手動チャンク最適化 (`manualChunks`)**:
  `vendor-react`、`vendor-syntax`、`data-chapters`、`data-guides` 等を用途別に分割し、ブラウザキャッシュヒット率を最大化。
- **CDN エッジキャッシュ**:
  Cloudflare Pages のグローバルエッジネットワークにより、国内・海外問わず数十msの超高速レスポンス（TTFB）を実現。

### 7.2 セキュリティ設計
- **XSS（クロスサイトスクリプティング）防止**:
  ユーザー入力（修了証の名前等）はすべて React の自動エスケープ機構を経由してレンダリング。
- **外部リンク安全性**:
  すべての外部サイト・姉妹サイトリンクに `rel="noopener noreferrer"` または `rel="noopener"` を付与し、Tabnabbing攻撃を防止。
- **進捗データ暗号化・サニタイズ**:
  localStorage からの読み込み時に厳格なスキーマ検証を行い、不正スクリプト注入を防止。

### 7.3 アクセシビリティ対応
- アイコンボタンに対する `aria-label`（目次を開く、閉じる等）の徹底。
- スクリーンリーダー向けのアウトライン構造（`h1`〜`h4` の見出し階層、`<nav>`、`<main>`、`<aside>`、`<footer>` のセマンティックタグ）。
- キーボードナビゲーション（Tabキーによるフォーカス移動とEnterキー押下）の保証。

### 7.4 解析・広告基盤
- **Google Analytics 4 (GA4)**:
  `G-SFRLEW9FPF` を導入。SPAのページ遷移イベント（`page_view`）をカスタム実装し、全章の読了率・滞在時間を正確に計測。
- **Google AdSense**:
  `ca-pub-7747700975082390` を導入。読者の学習を妨げない位置（フッター上部・関連書籍枠）に限定して最適配信。

---

## 8. ビルド・デプロイ・運用保守フロー

### 8.1 パッケージスクリプト一覧

| コマンド | 内容 | 実行タイミング |
| :--- | :--- | :--- |
| `npm run dev` | Vite開発サーバー起動 (HMR有効) | ローカル開発・機能追加時 |
| `npm run build` | 型検査 ＋ Vite本番ビルド ＋ SEO静的HTML生成 | 本番検証・デプロイ前 |
| `npm run preview` | `dist/` ディレクトリの本番シミュレーション | リリース前のローカル最終確認 |
| `npm run deploy` | ビルド ＋ Cloudflare Pages 本番公開 ＋ Git Push | 本番リリース時 |

### 8.2 ビルド & 静的生成パイプライン

```bash
# 実行されるコマンドの内部フロー
tsc && vite build && node scripts/generate-seo.mjs
```

1. **`tsc`**: TypeScript コンパイラによる全ソースコードの型整合性チェック。型エラーがあればビルド中断。
2. **`vite build`**: Rollupによるモジュール最適化、CSS抽出、Minify、Tree-shakingを実行し `dist/` を出力。
3. **`node scripts/generate-seo.mjs`**: 生成された `dist/index.html` をテンプレートとし、全44記事の事前生成HTML、RSSフィード、サイトマップを一括生成。

### 8.3 デプロイフロー (Cloudflare Pages + Git)

```bash
# デプロイコマンド
npm run deploy
```

上記コマンド一発で以下のタスクが完全自動実行されます：
1. **本番ビルド & SEO生成**
2. **Wrangler による Cloudflare Pages への直接デプロイ**:
   - プロジェクト名: `cpp-invader-study`
   - エッジCDNへ数秒で世界同時反映
3. **GitHub リモートリポジトリ（`main` ブランチ）への自動同期**:
   - `git push origin main` によるソースコード変更の保全

---

## 付録: 改版履歴

| 版数 | 改訂年月日 | 改訂者 | 改訂内容 |
| :---: | :---: | :---: | :--- |
| **1.0.0** | 2026-09-21 | シロクマC++ラボ開発チーム | 初版作成。システムアーキテクチャ、機能要件、データモデル、SEO静的フォールバック設計、姉妹サイト相互リンク仕様を完全文書化。 |
